#include <stdio.h>

/**
 * main - prints the whole alphabeti
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 97; x < 122; x++)
		putchar(x);

	for (x = 65; x <= 90; x++)
		putcha(x);
	putchar('\n');
	return (0);
}
