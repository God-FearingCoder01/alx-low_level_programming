#include<stdio.h>

/**
 * main - Entry point
 *
 * main - prints the various types on
 * the computer it is compiled and run on.
 * Return: Always 0 (Success)
 */
int main(void)
{
sizeof();
return (0);
}
